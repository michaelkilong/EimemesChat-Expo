// constants.ts — v1.0
// Set your Vercel deployment URL here. The Expo app calls the same
// serverless functions as the web app.
export const API_BASE = 'https://app-eimemeschat.vercel.app';

export const DAILY_LIMIT = 150;
export const MAX_MSGS    = 100;
export const AI_TIMEOUT  = 30000;
